/**
 * ─────────────────────────────────────────────────────────────────────────
 *  예약 가능 시간 설정
 *  이 파일만 고치면 됩니다. 나머지 코드는 건드릴 필요 없습니다.
 * ─────────────────────────────────────────────────────────────────────────
 */

/** 요일별 예약 가능 시간대 (24시간제, Asia/Seoul 기준)
 *  0=일요일, 1=월, 2=화, 3=수, 4=목, 5=금, 6=토
 *  한 요일에 여러 구간을 넣을 수도 있습니다: [[10,13],[15,20]]
 *  비워두면([]) 그 요일은 예약 불가.
 */
export const WEEKLY_WINDOWS: Record<number, [number, number][]> = {
  0: [[10, 20]], // 일요일 10:00 ~ 20:00
  1: [],         // 월요일 - 불가
  2: [],         // 화요일 - 불가
  3: [],         // 수요일 - 불가
  4: [],         // 목요일 - 불가
  5: [[19, 22]], // 금요일 19:00 ~ 22:00
  6: [[10, 20]], // 토요일 10:00 ~ 20:00
};

/** 약속 길이 (분). 현재는 1시간 고정. */
export const SLOT_MINUTES = 60;

/** 슬롯 시작 간격 (분). SLOT_MINUTES와 같으면 겹치지 않는 정시 슬롯. */
export const SLOT_STEP_MINUTES = 60;

/** 앞뒤로 확보할 여유 시간 (분).
 *  기존 일정과 이만큼 붙어있는 슬롯은 이동 시간 때문에 제외합니다. */
export const BUFFER_MINUTES = 30;

/** 최소 며칠 뒤부터 예약을 받을지 (시간 단위).
 *  24 = 24시간 이내의 슬롯은 노출하지 않음. */
export const MIN_NOTICE_HOURS = 24;

/** 며칠 앞까지 열어둘지 */
export const HORIZON_DAYS = 45;

/** 특정 날짜를 통째로 막기 (YYYY-MM-DD). 휴가, 개인 일정 등 */
export const BLOCKED_DATES: string[] = [
  // "2026-08-15",
];

/** 사이트에 표시할 정보 */
export const SITE = {
  title: "약속 잡기",
  ownerName: "지수",
  /** 상단 안내 문구 */
  intro:
    "아래에서 편한 시간을 골라 신청해 주세요. 확인 후 확정 안내를 메일로 보내드립니다.",
  /** 약속 1건 소요 시간 표시용 */
  durationLabel: "1시간",
};

/** 한국은 서머타임이 없어 UTC+9 고정입니다.
 *  다른 나라를 지원하려면 여기부터 손봐야 합니다. */
export const TZ = "Asia/Seoul";
export const TZ_OFFSET_HOURS = 9;
